NODE_ENV=stage
# Porta interna da API (container)
PORT=3000

DB_USER=agregamigos
DB_PASSWORD=initial123
DB_DATABASE=agregamigos
DB_HOST=127.0.0.1
DB_PORT=5457
DB_DIALECT=postgres

JWT_SECRET=
# Em Docker de producao, publicar a API na porta 4002 (ver docker-compose)
# JWT_EXPIRES_IN=7d
